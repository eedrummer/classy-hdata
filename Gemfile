source :gemcutter

# For the web interface
gem 'sinatra'
gem 'builder'

# For the data store
gem 'dm-core'
gem 'dm-validations'
gem 'dm-timestamps'
gem 'dm-aggregates'
gem 'do_sqlite3'

group :test do
  gem 'rack-test', :require => 'rack/test'
  gem 'thoughtbot-shoulda', :require => 'shoulda'
  gem 'nokogiri'
end
